version: "3.9"
services:
  backend:
    build: .
    depends_on:
      - redis
    env_file:
      - .env
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - backend
  redis:
    image: redis:7